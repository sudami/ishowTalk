<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">
    <!--  android:background="@color/theme_color" -->
<include layout="@layout/activity_titlebar"></include>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="?attr/actionBarSize"
        android:orientation="vertical">
        <RadioGroup
            android:id="@+id/RadioGroup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
           android:layout_marginTop="14dp"
            android:layout_marginBottom="14dp"
            android:background="@drawable/newright_title_3text"
            android:orientation="horizontal" >

            <RadioButton
                android:id="@+id/newAll"
                android:layout_width="wrap_content"
                android:layout_height="30dp"
                android:minWidth="70dp"
                android:gravity="center"
                android:text="全部"
                android:background="@drawable/select_members_left"
                android:button="@null"
                android:checked="true"
                android:textColor="@color/select_members_textcolor"
                android:textSize="14sp" />
            <TextView
                android:layout_width="1px"
                android:layout_height="match_parent"
                android:padding="10px"
                android:background="@color/colorPrimary"/>
            <RadioButton
                android:id="@+id/newGrade"
                android:layout_width="wrap_content"
                android:layout_height="30dp"
                android:gravity="center"
                android:background="@drawable/select_members_middle"
                android:text="本班"
                android:minWidth="70dp"
                android:button="@null"

                android:textColor="@color/select_members_textcolor"
                android:textSize="14sp" />

            <TextView
                android:layout_width="1px"
                android:layout_height="match_parent"
                android:padding="1px"
                android:background="@color/colorPrimary"/>

            <RadioButton
                android:id="@+id/newFans"
                android:layout_width="wrap_content"
                android:layout_height="30dp"
                android:gravity="center"
                android:text="关注"
                android:minWidth="70dp"
                android:background="@drawable/select_members_right"
                android:button="@null"
                android:textColor="@color/select_members_textcolor"
                android:textSize="14sp" />
        </RadioGroup>

        <FrameLayout

            android:id="@+id/newFrameLayout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            >
        </FrameLayout>
    </LinearLayout>


</android.support.design.widget.CoordinatorLayout>